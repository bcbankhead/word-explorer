extends ../layout

block content
    .main
      include ../partials/main
      .search
        include ../partials/user
        include ../partials/sform
        include ../partials/search
        if currentUser != 'new'
          include ../partials/profile
        #hidden
          if payload  
            !{payload}
          else
            |empty
